import React from "react";
import { Typography } from "@material-ui/core";

const SeminarNotFound = () => {
  return <Typography variant="h5">No seminar found!</Typography>;
};

SeminarNotFound.propTypes = {};

export default SeminarNotFound;
